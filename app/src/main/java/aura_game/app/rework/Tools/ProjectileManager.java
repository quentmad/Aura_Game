package aura_game.app.rework.Tools;


public class ProjectileManager {
    /*
    private List<Projectile> activeProjectiles;

    // ... autres méthodes, etc.

    public void addProjectile(Projectile projectile) {
        activeProjectiles.add(projectile);
    }

    public void updateProjectiles() {
        List<Projectile> updatedProjectiles = new ArrayList<>();

        for (Projectile projectile : activeProjectiles) {
            // Update the projectile's position
            projectile.updatePosition();

            // Check for collisions with all entities
            for (Entity entity : entities) {
                if (projectile.getHitbox().overlaps(entity.getHitbox())) {
                    // Handle the collision
                    projectile.handleCollision(entity);

                    // Mark the projectile for removal
                    projectile.setActive(false);
                    break;
                }
            }

            // If the projectile is still active, add it to the updated list
            if (projectile.isActive()) {
                updatedProjectiles.add(projectile);
            }
        }

        // Replace the list of active projectiles with the updated list
        activeProjectiles = updatedProjectiles;
    }
*/
    // ... autres méthodes, etc.
}

